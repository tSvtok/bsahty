@startuml

left to right direction

actor Guest
actor User
actor Admin

User --|> Guest
Admin --|> User

rectangle "B-SSAHTY Platform" {

  (Register)
  (Login)
  (Authenticate)

  (Edit Profile)
  (Change Name)
  (Change Image)
  (Change Bio)
  (Change City)
  (Update Favorite Sports)

  (View Feed)
  (Create Feed)

  (Post)
  (Comment)
  (Like)

  (Add Friends)
  (Chat)

  (Map & Spots)
  (Create Spot)

  (Create Event)
  (Join Event)

  (Admin Panel)
  (Approve Spots)
  (Ban Users)
}

' ========================
' Actors
' ========================
Guest -- (Register)
Guest -- (Login)

User -- (Edit Profile)
User -- (View Feed)
User -- (Create Feed)
User -- (Post)
User -- (Comment)
User -- (Like)
User -- (Add Friends)
User -- (Chat)
User -- (Map & Spots)
User -- (Create Spot)
User -- (Create Event)
User -- (Join Event)

Admin -- (Admin Panel)
Admin -- (Approve Spots)
Admin -- (Ban Users)

(View Feed) ..> (Authenticate)
(Create Feed) ..> (Authenticate)
(Post) ..> (Authenticate)
(Comment) ..> (Authenticate)
(Like) ..> (Authenticate)
(Add Friends) ..> (Authenticate)
(Chat) ..> (Authenticate)
(Map & Spots) ..> (Authenticate)
(Create Spot) ..> (Authenticate)
(Create Event) ..> (Authenticate)
(Join Event) ..> (Authenticate)
(Admin Panel) ..> (Authenticate)
(Approve Spots) ..> (Authenticate)
(Ban Users) ..> (Authenticate)
(Edit Profile) ..> (Authenticate)

' ========================
' EDIT PROFILE DECOMPOSITION (NO INCLUDE)
' ========================
(Edit Profile) <-- (Change Name)
(Edit Profile) <-- (Change Image)
(Edit Profile) <-- (Change Bio)
(Edit Profile) <-- (Change City)
(Edit Profile) <-- (Update Favorite Sports)

@enduml